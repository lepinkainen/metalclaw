[project]
name = "metalclaw"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.14"
dependencies = [
    "httpx>=0.28.1",
    "prompt_toolkit>=3.0",
    "rich>=13.0",
]

[dependency-groups]
dev = ["ruff", "pytest"]
